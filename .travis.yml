sudo: required
addons:
  chrome: stable

language: node_js
node_js:
  - stable

branches:
  only:
    - master

before_script:
  - npm install -g @angular/cli

script:
  - ng build --prod --aot

deploy:
  provider: pages
  skip-cleanup: true
  github-token: 6f6d07f7045e658f19fa05ea8ca4fecf5e57b48b
  keep-history: true
  on:
    branch: master
